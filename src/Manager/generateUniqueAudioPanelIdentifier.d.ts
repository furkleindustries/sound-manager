import { IBaseNode } from '../Node/IBaseNode';
export declare const strings: {
    NAME_INVALID: string;
    NODE_INVALID: string;
    NODE_TYPE_INVALID: string;
};
export declare function generateUniqueAudioPanelIdentifier(node: IBaseNode, name?: string): string;
