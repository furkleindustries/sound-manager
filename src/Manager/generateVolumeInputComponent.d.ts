import { IBaseNode } from '../Node/IBaseNode';
export declare const strings: {
    NODE_INVALID: string;
    UNIQUE_ID_INVALID: string;
};
export declare function generateVolumeInputComponent(node: IBaseNode, uniqueId: string): HTMLInputElement;
